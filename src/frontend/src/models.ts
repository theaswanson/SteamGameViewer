export interface AppInfo {
  title: string;
  description: string;
  releaseDate: string;
  developer: string;
  rating: number;
  currentPrice: number;
  retailPrice: number;
  headerImageUrl: string;
  libraryImageUrl: string;
  screenshotUrls: string[];
}